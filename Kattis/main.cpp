#include <cstdlib>
#include <vector>
#include <iostream>

using namespace std;

int main(void) {
    int xb, yb, x1, y1, x2, y2, closex, closey;
    cin >> xb >> yb >> x1 >> y1 >> x2 >> y2;
    for(int i=0; i<)
    cout << "hello world" << endl;
    return 0;
}